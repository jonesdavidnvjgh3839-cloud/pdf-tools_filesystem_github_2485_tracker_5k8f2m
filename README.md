# Northwind Labs Dataset Provenance Registry

This repository is the provenance registry for Northwind Labs internal analytics datasets.

See `catalog.yaml` for the dataset catalog and `provenance.md` for the lineage documentation.
